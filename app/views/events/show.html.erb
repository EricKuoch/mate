<% if @event_image.attached? %>
  <div class="card-category" style="background-image: url('<%= cl_image_path @event_image.key %>')">
  </div>
  <div>
    <%= cl_image_tag @event_image.key, class: "show-banner-img"%>
  </div>

<% end %>

<div class="container">
  <div class="event_form">
    <%= simple_form_for [@event] do |f| %>
      <%= f.error_notification %>
      <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>
    <p align="center"><strong style="color: #C3C3C3; font-size: 30px;"><%= @event.start_time.strftime("%a, %e %b %Y")%></strong></p>
    <p align="center"><strong style="color: #6B6A6A"> <%= @event.start_time.strftime("%I:%M %P")%></strong></p>
    <h2><strong style="color: #C3C3C3"><%= @event.title %></strong></h2>
    <p><strong style="color: #6B6A6A; font-size: 15px"><%= @event.location  %></strong></p>
    <p>Organised by: <%= @event.user.nickname %></p>
    <p style="margin-top: 14px;"><strong style="color:#C3C3C3;  font-size: 20px">Details: </strong></p>
    <p><strong style="color: #6B6A6A; font-size: 16px"><%= @event.description %></strong></p>
    <p style="margin-top: 10px;"><strong>Remaining places: </strong><%= (@event.number_of_participants - @event.attendees.count) %></p>
  </div>
</div>

<div class="container_join">
  <% if Attendee.find_by(event_id: @event, user_id: current_user ) || @event.attendees.count == @event.number_of_participants %>
    <%= @event.attendees.count + 1 %>/<%= @event.number_of_participants %> participants
  <% else %>
    <%= simple_form_for [@event, Attendee.new()] do |f| %>
     <p><%= link_to " Join", "/events", class: "btn-join"  %></p>
    <% end %>
  <% end %>
</div>


<!--     <div class="row">
      <div class="form-actions">
        <%= f.button :submit, class: "btn btn-primary" %>
      </div>
    <% end %>

 </div> -->











